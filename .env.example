# CORS
# Comma-separated list of allowed frontend origins
# Leave empty string to enable localhost wildcard (no credentials)
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:3010,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:3010,http://127.0.0.1:5173

# Rate limiting (requires slowapi). Set to "false" to disable.
RATE_LIMIT_ENABLED=true
# Default per-client limit (SlowAPI format, e.g., "120/minute", "1000/day")
RATE_LIMIT_DEFAULT=120/minute
